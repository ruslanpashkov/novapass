import type { ReactNode } from "react";

/**
 * Props for the RTL CacheProvider component
 */
export interface CacheProviderProps {
  /**
   * Child components to be wrapped with the RTL cache provider
   */
  children: ReactNode;
}
